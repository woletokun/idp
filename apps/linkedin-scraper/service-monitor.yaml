apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: linkedin-scraper-monitor
  namespace: apps
spec:
  selector:
    matchLabels:
      app: linkedin-scraper
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
